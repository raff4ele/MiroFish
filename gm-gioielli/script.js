(()=>{const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)],reduce=matchMedia('(prefers-reduced-motion:reduce)').matches,coarse=matchMedia('(pointer:coarse)').matches;const header=$('#siteHeader'),menu=$('#menuButton'),mobile=$('#mobileMenu');menu?.addEventListener('click',()=>{const o=mobile.classList.toggle('open');menu.classList.toggle('active',o);menu.setAttribute('aria-expanded',String(o));mobile.setAttribute('aria-hidden',String(!o))});$$('#mobileMenu a').forEach(a=>a.onclick=()=>{mobile.classList.remove('open');menu?.classList.remove('active')});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12,rootMargin:'0px 0px -4% 0px'});$$('.reveal').forEach(e=>io.observe(e));let counted=false;const metrics=$('.metrics-wrap');if(metrics)new IntersectionObserver(es=>{if(!es[0].isIntersecting||counted)return;counted=true;$$('[data-count]').forEach(el=>{const target=+el.dataset.count,start=performance.now();const tick=n=>{const p=Math.min(1,(n-start)/850);el.textContent=Math.round(target*(1-Math.pow(1-p,3)));if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)});$$('.metric-ring').forEach(r=>r.style.setProperty('--value',r.style.getPropertyValue('--p')||'.7'))},{threshold:.3}).observe(metrics);const story=$('#story'),steps=$$('.story-step'),art=$('.story-art'),route=$('#routePath');let scrollP=0;function onScroll(){header?.classList.toggle('scrolled',scrollY>45);scrollP=Math.max(0,scrollY/innerHeight);if(story&&innerWidth>720&&!reduce){const r=story.getBoundingClientRect(),total=story.offsetHeight-innerHeight,p=Math.min(1,Math.max(0,-r.top/Math.max(total,1))),i=Math.min(steps.length-1,Math.floor(p*steps.length));steps.forEach((s,j)=>s.classList.toggle('active',i===j));if(art){art.style.setProperty('--p',`${p*100}%`);art.style.setProperty('--r',`${p*125}deg`);art.style.setProperty('--s',`${.88+Math.sin(p*Math.PI)*.18}`);art.style.setProperty('--gx',`${Math.sin(p*Math.PI*2)*72}px`);art.style.setProperty('--gy',`${-165+p*120}px`);art.style.setProperty('--gr',`${-20+p*130}deg`)}}if(route){const m=route.closest('.map-wrap'),r=m?.getBoundingClientRect();if(r){const p=Math.min(1,Math.max(0,(innerHeight-r.top)/(innerHeight+r.height*.3)));route.style.setProperty('--route',String(240*(1-p)))}}}addEventListener('scroll',onScroll,{passive:true});onScroll();if(!coarse&&!reduce){$$('.tilt').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`rotateY(${x*8}deg) rotateX(${-y*7}deg) translateY(-5px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});$$('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.08}px,${(e.clientY-r.top-r.height/2)*.1}px)`});el.addEventListener('pointerleave',()=>el.style.transform='')})}$('#newsletterForm')?.addEventListener('submit',e=>{e.preventDefault();const mail=$('#email')?.value.trim();if(mail)location.href=`mailto:sales@gmgioielli.it?subject=${encodeURIComponent('GM Gioielli - richiesta informazioni')}&body=${encodeURIComponent('Buongiorno, vorrei ricevere informazioni e novità GM Gioielli.\n\nLa mia email: '+mail)}`});const canvas=$('#jewelCanvas');if(!canvas)return;const gl=canvas.getContext('webgl2',{antialias:true,alpha:true,powerPreference:'high-performance'});if(!gl){document.documentElement.classList.add('no-webgl');return}const vs=`#version 300 es\nin vec2 a;out vec2 v;void main(){v=a*.5+.5;gl_Position=vec4(a,0,1);}`;const fs=`#version 300 es\nprecision highp float;out vec4 o;in vec2 v;uniform vec2 r;uniform float t,s;uniform vec2 m;mat2 R(float a){float c=cos(a),d=sin(a);return mat2(c,-d,d,c);}float tor(vec3 p,vec2 q){vec2 k=vec2(length(p.xy)-q.x,p.z);return length(k)-q.y;}float oct(vec3 p,float k){p=abs(p);return(p.x+p.y+p.z-k)*.57735;}vec2 U(vec2 a,vec2 b){return a.x<b.x?a:b;}vec2 M(vec3 p){p.yz=R(.96+m.y*.1)*p.yz;p.xz=R(.18+s*.17+m.x*.18)*p.xz;vec2 h=vec2(tor(p,vec2(1.15,.074)),1.);vec3 b=p+vec3(.02,0,.07);h=U(h,vec2(tor(b,vec2(1.01,.04)),1.));vec3 g=p-vec3(0,1.13,0);g.yz=R(.45)*g.yz;g.xz=R(.4)*g.xz;h=U(h,vec2(oct(g,.31),2.));vec3 d=p-vec3(.52,.94,.08);d.yz=R(-.3)*d.yz;h=U(h,vec2(oct(d,.20),3.));return h;}vec3 N(vec3 p){vec2 e=vec2(.002,0);float d=M(p).x;return normalize(vec3(M(p+e.xyy).x-d,M(p+e.yxy).x-d,M(p+e.yyx).x-d));}float H(vec2 p){p=fract(p*vec2(123.4,345.6));p+=dot(p,p+34.5);return fract(p.x*p.y);}void main(){vec2 u=(gl_FragCoord.xy*2.-r)/r.y;u.x-=r.x/r.y>.9?.38:.04;vec3 ro=vec3(0,0,4.7),rd=normalize(vec3(u,-2.05));float z=0.,ma=0.;bool hit=false;for(int i=0;i<90;i++){vec2 h=M(ro+rd*z);if(h.x<.0017){hit=true;ma=h.y;break;}if(z>8.)break;z+=h.x*.72;}vec3 bg=vec3(.008,.007,.006);float st=step(.997,H(floor(gl_FragCoord.xy*.55+t)));bg+=vec3(.8,.28,.08)*st*.24;if(!hit){o=vec4(bg,1);return;}vec3 p=ro+rd*z,n=N(p),l=normalize(vec3(-2.5,3.4,4));float di=max(dot(n,l),0.),sp=pow(max(dot(reflect(-l,n),-rd),0.),70.),fr=pow(1.-max(dot(n,-rd),0.),3.);vec3 c;if(ma<1.5)c=vec3(.78,.29,.15)*(.33+di*.9)+vec3(1,.73,.46)*sp*1.8+vec3(.35,.08,.02)*fr;else if(ma<2.5)c=mix(vec3(.38,.08,.19),vec3(1,.55,.58),di)+vec3(.7,.8,1)*fr*.35+sp*vec3(1)*2.;else c=mix(vec3(.42,.16,.08),vec3(1,.61,.25),di)+vec3(1,.18,.08)*fr*.25+sp*vec3(1)*1.6;o=vec4(mix(bg,c,.97),1);}`;function sh(type,src){const x=gl.createShader(type);gl.shaderSource(x,src);gl.compileShader(x);if(!gl.getShaderParameter(x,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(x));return x}let pr;try{pr=gl.createProgram();gl.attachShader(pr,sh(gl.VERTEX_SHADER,vs));gl.attachShader(pr,sh(gl.FRAGMENT_SHADER,fs));gl.linkProgram(pr);if(!gl.getProgramParameter(pr,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(pr))}catch(e){console.warn(e);return}const b=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([-1,-1,3,-1,-1,3]),gl.STATIC_DRAW);gl.useProgram(pr);const A=gl.getAttribLocation(pr,'a');gl.enableVertexAttribArray(A);gl.vertexAttribPointer(A,2,gl.FLOAT,false,0,0);const ur=gl.getUniformLocation(pr,'r'),ut=gl.getUniformLocation(pr,'t'),us=gl.getUniformLocation(pr,'s'),um=gl.getUniformLocation(pr,'m'),pt={x:0,y:0,tx:0,ty:0};addEventListener('pointermove',e=>{pt.tx=(e.clientX/innerWidth-.5)*2;pt.ty=(e.clientY/innerHeight-.5)*2},{passive:true});function resize(){const d=Math.min(devicePixelRatio||1,coarse?1.15:1.6),w=Math.floor(canvas.clientWidth*d),h=Math.floor(canvas.clientHeight*d);if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;gl.viewport(0,0,w,h)}}const start=performance.now();function frame(now){resize();pt.x+=(pt.tx-pt.x)*.04;pt.y+=(pt.ty-pt.y)*.04;gl.uniform2f(ur,canvas.width,canvas.height);gl.uniform1f(ut,(now-start)/1000);gl.uniform1f(us,reduce?0:Math.min(scrollP,.9));gl.uniform2f(um,reduce?0:pt.x,reduce?0:pt.y);gl.drawArrays(gl.TRIANGLES,0,3);requestAnimationFrame(frame)}requestAnimationFrame(frame)})();